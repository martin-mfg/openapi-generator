openapi: 3.0.0
info:
  title: dummy
  version: dummy
  description: dummy
paths:
  /example/someMethod:
    get:
      description: dummy
      operationId: dummy
      responses:
        200:
          description: dummy
          content:
            "*/*":
              schema:
                $ref: '#/components/schemas/ExampleResponse'
components:
  schemas:
    ExampleResponse:
      type: object
      description: dummy
      properties:
        myOnlyProperty:
          $ref: '#/components/schemas/OtherObject'
    OtherObject:
      type: object
      description: dummy
      properties:
        myOnlyProperty2:
          $ref: '#/components/schemas/ExampleResponse'
