openapi: 3.0.0
info:
  title: dummy
  version: dumy
  description: dummy
paths:
  /example/someMethod:
    get:
      description: dummy
      operationId: dummy
      responses:
        200:
          description: dummy
          content:
            "*/*":
              schema:
                $ref: '#/components/schemas/ExampleResponse'
components:
  schemas:
    ExampleResponse:
      type: object
      description: dummy
      properties:
        myOnlyProperty:
          type: array
          uniqueItems: true
          items:
            type: number
